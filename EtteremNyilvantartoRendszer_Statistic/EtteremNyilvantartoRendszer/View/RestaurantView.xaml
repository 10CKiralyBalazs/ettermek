<UserControl x:Class="EtteremNyilvantartoRendszer.View.RestaurantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EtteremNyilvantartoRendszer.View"
             xmlns:viewmodel="clr-namespace:EtteremNyilvantartoRendszer.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="Silver">
    <UserControl.DataContext>
        <viewmodel:RestaurantViewModel />
    </UserControl.DataContext>
    <StackPanel Margin="10">
        <Label>Éttermek</Label>
        <ListView ItemsSource="{Binding Restaurants}" SelectedItem="{Binding SelectedRestaurant, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Azonosító" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Étterem Neve" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Étterem Tulajdonosa" DisplayMemberBinding="{Binding OwnerName}" />
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
